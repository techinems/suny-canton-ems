This is a Next.js application that uses Mantine for its UI components. It also uses tabler for its icons. All components are stored in src/components. Directus is used for the data store.

The purpose of this application is to create a web app for an EMS organization that allows for managing certifications, calls, and inventory.